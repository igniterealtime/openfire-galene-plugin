# Client-Side Development

> **Relevant source files**
> * [src/root/static/background-blur-worker.js](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/background-blur-worker.js)
> * [src/root/static/galene.js](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js)
> * [src/root/static/index.html](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/index.html)
> * [src/root/static/mainpage.css](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/mainpage.css)
> * [src/root/static/mainpage.js](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/mainpage.js)
> * [src/root/static/stats.html](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/stats.html)
> * [src/root/static/stats.js](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/stats.js)
> * [src/root/static/tsconfig.json](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/tsconfig.json)

This document covers the web client architecture, JavaScript components, and client-side features of the Galene video conferencing interface. It focuses on the browser-based user interface that connects to the embedded Galene SFU through WebSocket proxying.

For server-side plugin development, see [Plugin Development Overview](/igniterealtime/openfire-galene-plugin/6.1-plugin-development-overview). For WebSocket proxy implementation details, see [WebSocket Implementation Details](/igniterealtime/openfire-galene-plugin/6.2-websocket-implementation-details). For administration interfaces, see [Admin Console Interface](/igniterealtime/openfire-galene-plugin/3.1-admin-console-interface).

## Web Client Architecture Overview

The Galene web client is a single-page application built around WebRTC peer connections and a custom signaling protocol. The main entry points are the landing page and the group interface, with the core functionality implemented in JavaScript modules.

```mermaid
flowchart TD

GALENE_CSS["galene.css<br>Conference Styles"]
COMMON_CSS["common.css<br>Shared Styles"]
MAINPAGE_CSS["mainpage.css<br>Landing Styles"]
INDEX["index.html<br>Landing Page"]
GALENE_HTML["galene.html<br>Conference Interface"]
STATS_HTML["stats.html<br>Statistics View"]
GALENE_JS["galene.js<br>Main Conference Logic"]
PROTOCOL_JS["protocol.js<br>WebSocket Protocol"]
MAINPAGE_JS["mainpage.js<br>Landing Page Logic"]
STATS_JS["stats.js<br>Statistics Display"]
BLUR_WORKER["background-blur-worker.js<br>MediaPipe Integration"]
VISION_BUNDLE["/third-party/tasks-vision/<br>MediaPipe Library"]

INDEX --> MAINPAGE_JS
GALENE_HTML --> GALENE_JS
GALENE_JS --> BLUR_WORKER
STATS_HTML --> STATS_JS

subgraph subGraph2 ["Background Processing"]
    BLUR_WORKER
    VISION_BUNDLE
    BLUR_WORKER --> VISION_BUNDLE
end

subgraph subGraph1 ["Core JavaScript Modules"]
    GALENE_JS
    PROTOCOL_JS
    MAINPAGE_JS
    STATS_JS
    GALENE_JS --> PROTOCOL_JS
end

subgraph subGraph0 ["HTML Pages"]
    INDEX
    GALENE_HTML
    STATS_HTML
end

subgraph subGraph3 ["Styling and Assets"]
    GALENE_CSS
    COMMON_CSS
    MAINPAGE_CSS
end
```

The web client operates as a WebRTC application that communicates with the Galene SFU through WebSocket connections proxied by the Openfire plugin. The main conference interface is built around the `ServerConnection` class and media stream management.

**Sources:** [src/root/static/galene.js L1-L40](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L1-L40)

 [src/root/static/index.html L1-L40](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/index.html#L1-L40)

 [src/root/static/background-blur-worker.js L1-L25](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/background-blur-worker.js#L1-L25)

## Core JavaScript Components

The main client logic is organized around several key JavaScript classes and global objects that manage different aspects of the video conferencing experience.

```mermaid
flowchart TD

NEW_UP_STREAM["newUpStream()<br>Create Upload Stream"]
ADD_LOCAL_MEDIA["addLocalMedia()<br>Capture Local Media"]
SET_MEDIA["setMedia()<br>Display Remote Stream"]
CLOSE_UP_MEDIA["closeUpMedia()<br>Stop Upload Stream"]
GET_SETTINGS["getSettings()<br>Retrieve Stored Settings"]
REFLECT_SETTINGS["reflectSettings()<br>Apply to UI"]
UPDATE_SETTINGS["updateSettings()<br>Modify Settings"]
STORE_SETTINGS["storeSettings()<br>Persist to Storage"]
GROUP_VAR["group<br>string: Group Name"]
SERVER_CONN["serverConnection<br>ServerConnection Instance"]
GROUP_STATUS["groupStatus<br>Object: Server Status"]
TOKEN_VAR["token<br>string: Auth Token"]
GOT_CONNECTED["gotConnected()<br>Connection Established"]
GOT_CLOSE["gotClose()<br>Connection Closed"]
JOIN_FUNC["join()<br>Join Group"]
SET_CONNECTED["setConnected()<br>Update UI State"]

SERVER_CONN --> GOT_CONNECTED
SERVER_CONN --> GOT_CLOSE

subgraph subGraph3 ["Connection Lifecycle"]
    GOT_CONNECTED
    GOT_CLOSE
    JOIN_FUNC
    SET_CONNECTED
    GOT_CONNECTED --> JOIN_FUNC
end

subgraph subGraph0 ["Global State Management"]
    GROUP_VAR
    SERVER_CONN
    GROUP_STATUS
    TOKEN_VAR
end

subgraph subGraph2 ["Media Management"]
    NEW_UP_STREAM
    ADD_LOCAL_MEDIA
    SET_MEDIA
    CLOSE_UP_MEDIA
    NEW_UP_STREAM --> ADD_LOCAL_MEDIA
end

subgraph subGraph1 ["Settings and Configuration"]
    GET_SETTINGS
    REFLECT_SETTINGS
    UPDATE_SETTINGS
    STORE_SETTINGS
    GET_SETTINGS --> REFLECT_SETTINGS
    UPDATE_SETTINGS --> STORE_SETTINGS
end
```

The architecture follows a pattern where global state variables track the connection status and group information, while functions handle specific operations like media management and settings persistence.

**Sources:** [src/root/static/galene.js L23-L66](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L23-L66)

 [src/root/static/galene.js L100-L164](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L100-L164)

 [src/root/static/galene.js L360-L468](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L360-L468)

## WebRTC Stream Management

The client implements a comprehensive WebRTC stream management system that handles both incoming and outgoing media streams, with support for simulcast, bitrate control, and multiple video sources.

```mermaid
flowchart TD

STREAM_UP["c.up = true<br>Outbound Stream"]
STREAM_DOWN["c.up = false<br>Inbound Stream"]
ON_TRACK["c.ondowntrack<br>Receive Remote Track"]
ON_STATUS["c.onstatus<br>Connection Status Updates"]
NEW_UP["newUpStream(localId)<br>Create Outbound Stream"]
GET_USER_MEDIA["getUserMedia()<br>Capture Camera/Mic"]
GET_DISPLAY_MEDIA["getDisplayMedia()<br>Screen Sharing"]
SET_FILTER["setFilter(c)<br>Apply Video Filter"]
REMOVE_FILTER["removeFilter(c)<br>Remove Video Filter"]
FILTER_CLASS["Filter Class<br>Canvas-based Processing"]
REPLACE_STREAM["replaceUpStream(c)<br>Replace Media Source"]
SET_SEND_PARAMS["setSendParameters(c, bps, simulcast)<br>Configure Encoding"]
GET_MAX_THROUGHPUT["getMaxVideoThroughput()<br>Quality Settings"]
RECONSIDER_PARAMS["reconsiderSendParameters()<br>Update All Streams"]
DO_SIMULCAST["doSimulcast()<br>Check Simulcast Support"]

GET_USER_MEDIA --> SET_FILTER
NEW_UP --> SET_SEND_PARAMS

subgraph subGraph2 ["Bitrate and Quality Control"]
    SET_SEND_PARAMS
    GET_MAX_THROUGHPUT
    RECONSIDER_PARAMS
    DO_SIMULCAST
    GET_MAX_THROUGHPUT --> SET_SEND_PARAMS
    SET_SEND_PARAMS --> RECONSIDER_PARAMS
end

subgraph subGraph1 ["Stream Processing"]
    SET_FILTER
    REMOVE_FILTER
    FILTER_CLASS
    REPLACE_STREAM
    SET_FILTER --> FILTER_CLASS
    FILTER_CLASS --> REPLACE_STREAM
end

subgraph subGraph0 ["Stream Creation"]
    NEW_UP
    GET_USER_MEDIA
    GET_DISPLAY_MEDIA
    NEW_UP --> GET_USER_MEDIA
    NEW_UP --> GET_DISPLAY_MEDIA
end

subgraph subGraph3 ["Connection Management"]
    STREAM_UP
    STREAM_DOWN
    ON_TRACK
    ON_STATUS
end
```

The stream management system supports advanced features like background blur through the `Filter` class, which uses canvas-based video processing to apply real-time effects to outgoing video streams.

**Sources:** [src/root/static/galene.js L1032-L1076](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L1032-L1076)

 [src/root/static/galene.js L1112-L1276](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L1112-L1276)

 [src/root/static/galene.js L720-L753](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L720-L753)

## Video Filtering and Background Blur

The client implements a sophisticated video filtering system centered around the `Filter` class and a Web Worker for background blur processing using MediaPipe.

```mermaid
flowchart TD

FILTER_CLASS["Filter Class<br>Canvas-based Processing"]
FILTER_DEF["filterDefinition<br>Object: Filter Configuration"]
FILTERS_GLOBAL["filters<br>Global Filter Registry"]
FILTER_SELECT["filterselect<br>UI Select Element"]
BLUR_WORKER["background-blur-worker.js<br>Web Worker"]
LOAD_SEGMENTER["loadImageSegmenter(model)<br>Initialize MediaPipe"]
FOREGROUND_MASK["foregroundMask(bitmap, timestamp)<br>Generate Alpha Mask"]
IMAGE_SEGMENTER["imageSegmenter<br>MediaPipe Instance"]
CANVAS_DRAW["Filter.prototype.draw()<br>Frame Processing"]
CAPTURE_STREAM["canvas.captureStream()<br>Output Stream"]
REQUEST_FRAME["requestFrame()<br>Manual Frame Capture"]
WORKER_MESSAGE["workerSendReceive()<br>Worker Communication"]
SET_FILTER_FUNC["setFilter(c)<br>Apply to Stream"]
REMOVE_FILTER_FUNC["removeFilter(c)<br>Clean Up Filter"]
REPLACE_UP_STREAM["replaceUpStream(c)<br>Update Connection"]
USER_DATA["c.userdata.filter<br>Filter Instance Storage"]

FILTER_CLASS --> CANVAS_DRAW
WORKER_MESSAGE --> FOREGROUND_MASK
FILTER_SELECT --> SET_FILTER_FUNC
SET_FILTER_FUNC --> FILTER_CLASS

subgraph subGraph3 ["Integration Points"]
    SET_FILTER_FUNC
    REMOVE_FILTER_FUNC
    REPLACE_UP_STREAM
    USER_DATA
    REMOVE_FILTER_FUNC --> USER_DATA
end

subgraph subGraph2 ["Processing Pipeline"]
    CANVAS_DRAW
    CAPTURE_STREAM
    REQUEST_FRAME
    WORKER_MESSAGE
    CANVAS_DRAW --> CAPTURE_STREAM
    CAPTURE_STREAM --> REQUEST_FRAME
    CANVAS_DRAW --> WORKER_MESSAGE
end

subgraph subGraph1 ["Background Blur Implementation"]
    BLUR_WORKER
    LOAD_SEGMENTER
    FOREGROUND_MASK
    IMAGE_SEGMENTER
    BLUR_WORKER --> LOAD_SEGMENTER
    LOAD_SEGMENTER --> IMAGE_SEGMENTER
    IMAGE_SEGMENTER --> FOREGROUND_MASK
end

subgraph subGraph0 ["Filter Infrastructure"]
    FILTER_CLASS
    FILTER_DEF
    FILTERS_GLOBAL
    FILTER_SELECT
end
```

The background blur feature uses MediaPipe's image segmentation running in a Web Worker to generate foreground masks that are applied in real-time to the video stream through canvas compositing.

**Sources:** [src/root/static/galene.js L1122-L1276](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L1122-L1276)

 [src/root/static/background-blur-worker.js L25-L93](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/background-blur-worker.js#L25-L93)

 [src/root/static/galene.js L704-L717](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L704-L717)

## UI Components and Settings Management

The client provides a comprehensive settings system that persists user preferences and reflects them in the user interface through HTML form controls and programmatic updates.

| Setting Category | UI Elements | Storage Functions | Settings Keys |
| --- | --- | --- | --- |
| Media Devices | `videoselect`, `audioselect` | `updateSettings()` | `video`, `audio` |
| Video Quality | `sendselect`, `simulcastselect` | `updateSetting()` | `send`, `simulcast` |
| Display Options | `mirrorbox`, `blackboardbox` | `reflectSettings()` | `mirrorView`, `blackboardMode` |
| Audio Processing | `hqaudiobox`, `preprocessingbox` | `getSettings()` | `hqaudio`, `preprocessing` |
| Activity Detection | `activitybox`, `displayallbox` | `storeSettings()` | `activityDetection`, `displayAll` |
| Video Filters | `filterselect` | `delSetting()` | `filter` |

```mermaid
flowchart TD

SESSION_STORAGE["sessionStorage<br>Browser Storage"]
FALLBACK_SETTINGS["fallbackSettings<br>Memory Fallback"]
GET_SETTINGS_FUNC["getSettings()<br>Retrieve Configuration"]
STORE_SETTINGS_FUNC["storeSettings(settings)<br>Persist Changes"]
REFLECT_SETTINGS_FUNC["reflectSettings()<br>Update UI Elements"]
GET_SELECT_ELEMENT["getSelectElement(id)<br>Type-safe DOM Access"]
GET_INPUT_ELEMENT["getInputElement(id)<br>Validate Input Elements"]
GET_BUTTON_ELEMENT["getButtonElement(id)<br>Button Element Access"]
VIDEO_SELECT_CHANGE["videoselect.onchange<br>Device Selection"]
FILTER_SELECT_CHANGE["filterselect.onchange<br>Filter Application"]
CHECKBOX_CHANGES["checkbox.onchange<br>Boolean Settings"]
BUTTON_CLICKS["button.onclick<br>Action Triggers"]

GET_SELECT_ELEMENT --> VIDEO_SELECT_CHANGE
VIDEO_SELECT_CHANGE --> STORE_SETTINGS_FUNC
FILTER_SELECT_CHANGE --> STORE_SETTINGS_FUNC
CHECKBOX_CHANGES --> STORE_SETTINGS_FUNC

subgraph subGraph2 ["Event Handlers"]
    VIDEO_SELECT_CHANGE
    FILTER_SELECT_CHANGE
    CHECKBOX_CHANGES
    BUTTON_CLICKS
end

subgraph subGraph1 ["UI Reflection"]
    REFLECT_SETTINGS_FUNC
    GET_SELECT_ELEMENT
    GET_INPUT_ELEMENT
    GET_BUTTON_ELEMENT
    REFLECT_SETTINGS_FUNC --> GET_SELECT_ELEMENT
    REFLECT_SETTINGS_FUNC --> GET_INPUT_ELEMENT
end

subgraph subGraph0 ["Settings Storage"]
    SESSION_STORAGE
    FALLBACK_SETTINGS
    GET_SETTINGS_FUNC
    STORE_SETTINGS_FUNC
    GET_SETTINGS_FUNC --> SESSION_STORAGE
    SESSION_STORAGE --> FALLBACK_SETTINGS
    STORE_SETTINGS_FUNC --> SESSION_STORAGE
end
```

The settings system provides type-safe DOM element access through helper functions and automatic persistence of user preferences, with fallback handling for environments where session storage is not available.

**Sources:** [src/root/static/galene.js L100-L164](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L100-L164)

 [src/root/static/galene.js L166-L200](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L166-L200)

 [src/root/static/galene.js L204-L302](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L204-L302)

 [src/root/static/galene.js L639-L686](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L639-L686)

## Connection and Protocol Handling

The client manages WebSocket connections to the Galene SFU through a protocol layer that handles authentication, group joining, and real-time communication with the server.

```mermaid
flowchart TD

SERVER_CONNECTION["serverConnection<br>ServerConnection Instance"]
ON_PEER_CONNECTION["onPeerConnection()<br>RTCConfiguration Override"]
REQUEST_FUNC["serverConnection.request(mapping)<br>Stream Requests"]
MAP_REQUEST["mapRequest(what)<br>Protocol Mapping"]
START_FUNC["start()<br>Initialize Connection"]
GOT_CONNECTED_FUNC["gotConnected()<br>WebSocket Established"]
JOIN_FUNC["join()<br>Join Group with Credentials"]
GOT_CLOSE_FUNC["gotClose(code, reason)<br>Handle Disconnection"]
TOKEN_AUTH["Token Authentication<br>token credential type"]
PASSWORD_AUTH["Password Authentication<br>authServer credential type"]
PROBING_STATE["probingState<br>Login State Machine"]
SET_CHANGE_PASSWORD["setChangePassword(username)<br>Password Link"]
SET_CONNECTED_FUNC["setConnected(connected)<br>Update Interface"]
SET_BUTTONS_VISIBILITY["setButtonsVisibility()<br>Show/Hide Controls"]
DISPLAY_ERROR["displayError(message)<br>Show Error Messages"]
CLEAR_CHAT["clearChat()<br>Reset Chat Interface"]

JOIN_FUNC --> TOKEN_AUTH
JOIN_FUNC --> PASSWORD_AUTH
GOT_CONNECTED_FUNC --> SET_CONNECTED_FUNC
GOT_CLOSE_FUNC --> DISPLAY_ERROR

subgraph subGraph3 ["UI State Updates"]
    SET_CONNECTED_FUNC
    SET_BUTTONS_VISIBILITY
    DISPLAY_ERROR
    CLEAR_CHAT
    SET_CONNECTED_FUNC --> SET_BUTTONS_VISIBILITY
    SET_CONNECTED_FUNC --> CLEAR_CHAT
end

subgraph subGraph1 ["Authentication Flow"]
    TOKEN_AUTH
    PASSWORD_AUTH
    PROBING_STATE
    SET_CHANGE_PASSWORD
    TOKEN_AUTH --> PROBING_STATE
    PASSWORD_AUTH --> SET_CHANGE_PASSWORD
end

subgraph subGraph0 ["Connection Lifecycle"]
    START_FUNC
    GOT_CONNECTED_FUNC
    JOIN_FUNC
    GOT_CLOSE_FUNC
    START_FUNC --> GOT_CONNECTED_FUNC
    GOT_CONNECTED_FUNC --> JOIN_FUNC
end

subgraph subGraph2 ["Protocol Communication"]
    SERVER_CONNECTION
    ON_PEER_CONNECTION
    REQUEST_FUNC
    MAP_REQUEST
    SERVER_CONNECTION --> ON_PEER_CONNECTION
    SERVER_CONNECTION --> REQUEST_FUNC
    REQUEST_FUNC --> MAP_REQUEST
end
```

The connection system supports multiple authentication methods including JWT tokens and password-based authentication through an auth server, with automatic UI state management based on connection status and user permissions.

**Sources:** [src/root/static/galene.js L384-L468](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L384-L468)

 [src/root/static/galene.js L470-L530](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L470-L530)

 [src/root/static/galene.js L584-L614](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L584-L614)

 [src/root/static/galene.js L796-L803](https://github.com/igniterealtime/openfire-galene-plugin/blob/5aa54ac8/src/root/static/galene.js#L796-L803)